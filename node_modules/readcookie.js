
exports.cook = (req) => {

    var cookie = require('cookie');
    // Parse the cookies on the request
    var cookies = cookie.parse(req.headers.cookie || '');
    // Get the visitor name set in the cookie
    var obra = cookies.obra;
    
    return obra;
}








